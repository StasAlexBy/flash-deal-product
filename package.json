{
  "name": "my-new-app",
  "version": "1.0.0",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy",
    "build:tw": "tailwindcss -i ./tailwind.css -o ./extensions/my-new-section/assets/tailwind.css --minify",
    "watch:tw": "tailwindcss -i ./tailwind.css -o ./extensions/my-new-section/assets/tailwind.css --watch"
  },
  "trustedDependencies": [
    "@shopify/plugin-cloudflare"
  ],
  "author": "stas",
  "private": true,
  "workspaces": [
    "extensions/*"
  ],
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  }
}
